# SPDX-License-Identifier: AGPL-3.0-or-later
# Bake the LB config into the image instead of bind-mounting it, so the harness
# also works under docker-outside-of-docker (where the daemon can't see a
# devcontainer's bind-mount paths).
FROM nginx:1.27-alpine
COPY nginx.conf /etc/nginx/nginx.conf
